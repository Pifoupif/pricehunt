function refreshForm() {
  const form = document.getElementById('new_alert');
  form.innerHTML = '<%= j render "shared/form-alert-creation", product: @product, alert: @alert %>';
}

<% if @alert.errors.any? %>
  refreshForm();
<% else %>
  window.location = "<%= alerts_path %>"
<% end %>

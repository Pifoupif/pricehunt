
function refreshSignInForm() {
  const form = document.getElementById('user_email');
  form.innerHTML = '<%= j render "shared/form-sign-in" %>';
}

<% if @user.errors.any? %>
  refreshSignInForm();
  <% raise %>
<% else %>
  window.location = "<%= alerts_path %>"
<% end %>
